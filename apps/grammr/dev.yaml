apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grammr-dev
  namespace: flux-system
spec:
  dependsOn:
    - name: shared
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: grammr
  path: ./deploy/dev
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age

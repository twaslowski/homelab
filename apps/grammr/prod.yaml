apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grammr-prod
  namespace: flux-system
spec:
  dependsOn:
    - name: shared
    - name: dev
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: grammr
  path: ./deploy/prod
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age

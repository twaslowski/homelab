apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: scheduled-backup
spec:
  cluster:
    name: vaultwarden-pg
  method: plugin
  schedule: "0 0 */6 * * *"
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backup-store
spec:
  configuration:
    destinationPath: s3://246770851643-eu-central-1-homelab-cluster-storage/backups/vaultwarden/vaultwarden-pg/
    endpointURL: https://s3.eu-central-1.amazonaws.com
    s3Credentials:
      accessKeyId:
        name: barman-aws-creds
        key: aws_access_key_id
      secretAccessKey:
        name: barman-aws-creds
        key: aws_secret_access_key
    wal:
      compression: gzip
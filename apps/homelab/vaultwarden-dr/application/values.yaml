domain: https://warden-dr.twaslowski.com

ingress:
  class: "cloudflare-tunnel"
  enabled: true
  hostname: warden-dr.twaslowski.com
  tls: false

database:
  type: postgresql
  existingSecret: pg-secret
  existingSecretKey: uri